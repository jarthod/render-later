<!DOCTYPE html>
<html>
  <head>
    <title>RenderLater</title>
    <style>
      body {
        font-family: San Francisco, Roboto, Ubuntu, sans-serif;
        background-color: #efecea;
        color: #432;
        width: 350px;
        margin: 1em auto;
      }

      header {
        color: #987;
        font-size: 2em;
        padding: 0.5em;
        text-align: center;
      }

      footer {
        border-top: 1px solid #dfdad6;
        color: #987;
        font-size: 0.9em;
        padding-top: 1em;
        margin-top: 1em;
        text-align: center;
      }

      ul {
        margin: 0;
        padding: 0;
      }

      li {
        list-style: none;
        margin: 0;
        background-color: white;
        padding: 10px;
        margin: 5px 0;
        display: flex;
      }

      li > h2, li > div {
      }

      li > h2 {
        font-size: 1em;
        margin: 0;
        width: 5em;
      }

      li .ip {
        text-align: center;
        color: #a98;
      }

      li .uptime {
        flex-grow: 1;
        text-align: right;
        color: #6d0;
      }
    </style>
  </head>
  <body>
    <header>render-later</header>
    <%= yield %>
    <footer>Adrien Jarthon — v<%= RenderLater::VERSION %></footer>
    <%= render_now %>
  </body>
</html>
